# SPDX-FileCopyrightText: Copyright (c) 2023 - 2026 NVIDIA CORPORATION & AFFILIATES.
# SPDX-FileCopyrightText: All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# ---------------------------------------------------------------------------
# Unified External Aerodynamics - Volume Training Configuration (DoMINO)
# ---------------------------------------------------------------------------
#
# >>> DRAFT: this config does NOT run end-to-end yet.
#
# The dataset YAML (drivaer_ml_volume.yaml) does not yet expose the
# volume-grid / neighbor features that DoMINO's `forward()` consumes.
# The `forward_kwargs:` block below is illustrative; running this config
# requires extending the dataset pipeline with a DoMINO-specific
# pre-processing stage (and is excluded from the synthetic E2E test
# suite for that reason). See `tests/test_synthetic_configs.py`.
#
# DoMINO uses a geometry-encoding UNet + basis-function architecture that
# natively handles SDF grids and volume neighbor information. Its forward
# method accepts a single ``data_dict`` argument; the recipe's
# ``forward_kwargs:`` resolver builds that dict by recursing into the
# nested-dict spec below (see ``src/forward_kwargs.py``).

output_dir: "runs"
checkpoint_dir: null
run_id: "volume_domino_drivaer_ml"

precision: "bfloat16"
compile: false            # DoMINO internals are not compile-friendly yet
profile: false
augment: false
benchmark_io: false

# -- Data contract -----------------------------------------------------------
# NOTE: DoMINO is still in progress in this recipe (the dataset pipeline
# does not yet expose all the volume-grid / neighbor features DoMINO's full
# forward expects). The spec below is illustrative; see the surface variant
# for context.
input_type: tensors
output_type: tensors

forward_kwargs:
  data_dict:
    geometry_coordinates: interior.points
    volume_mesh_centers: interior.points
    sdf_nodes: interior.point_data.sdf
    global_params_values: global_data.U_inf
    global_params_reference: global_data.U_inf

# -- Model (DoMINO, volume-only) --------------------------------------------
model:
  _target_: physicsnemo.models.domino.DoMINO
  input_features: 3         # x, y, z coordinates
  output_features_vol: 5    # velocity (3) + pressure (1) + nut (1)
  output_features_surf: null # volume-only mode
  global_features: 3        # freestream velocity U_inf (3 components)
  model_parameters:
    interp_res: [128, 64, 64]
    use_sdf_in_basis_func: true
    num_neighbors_volume: 10
    geometry_encoding_type: "both"
    solution_calculation_mode: "two-loop"

# -- Dataset -----------------------------------------------------------------
dataset:
  name: drivaer_ml_volume
  sampling_resolution: 200000
  metrics: [l2]

# -- Training ----------------------------------------------------------------
training:
  seed: 42
  num_epochs: 500
  save_interval: 25
  loss_type: "huber"
  batch_size: 1
  scheduler_update_mode: "epoch"

  optimizer:
    lr: 1.0e-3
    weight_decay: 1.0e-4
    betas: [0.9, 0.999]
    eps: 1.0e-8

  scheduler:
    _target_: torch.optim.lr_scheduler.StepLR
    step_size: 100
    gamma: 0.5

# -- DataLoader / Dataset Performance Tuning --------------------------------
dataloader:
  prefetch_factor: 1
  num_streams: 1
  use_streams: false       # SDF computation is not stream-safe
  num_workers: 1
  pin_memory: true

# -- Logging -----------------------------------------------------------------
logging:
  log_every_n_steps: 10

# -- Data paths --------------------------------------------------------------
# Splits are read from manifest.json; change train_split / val_split to
# switch experiments.
data:
  drivaer_ml:
    config: conf/dataset/drivaer_ml_volume.yaml
    train_split: train
    val_split: val
